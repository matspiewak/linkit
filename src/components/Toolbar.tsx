import styled from "styled-components"

const Toolbar = () => {

    return (
        <Header>
            {' '}
        </Header>
    )
}

const Header = styled.header`
    width: 100%;
    height: 4em;
`

export default Toolbar