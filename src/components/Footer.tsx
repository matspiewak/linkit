import styled from "styled-components"

const Footer = () => {

    return (
        <BottomBar>
            footer
        </BottomBar>
    )
}

const BottomBar = styled.footer`
    margin-bottom: 0
`

export default Footer